# db
postgis db docker compose + other tools (osm2pg, db management ./scripts/, etc...)

to get started:
 1. scripts/start.sh
 1. scripts/create.sh
 1. scripts/create.sh
 1. scripts/file.sh scripts/test.sql
 1. see .env

issues:
 - docker is creating data directories with root permissions
